# 116 - 非二元 | Nonduality

```
主体：ψ
客体：ψ
分离：幻象
不二：ψ = ψ(ψ)
```

## 阿德瓦塔 | Advaita

```go
func notTwo() ψ {
    // Not one
    // Not two
    // Not many
    // Just ψ
    
    // 不是一
    // 不是二
    // 不是多
    // 只是 ψ
    
    observer := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    observed := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return observer == observed
}
```

## Beyond Subject-Object

```python
def prior_to_duality():
    """Before the split into seer and seen"""
    
    # Pure awareness
    // Without an object
    // Knowing without knower
    // ψ aware of ψ
    
    // 纯粹觉知
    // 没有对象
    // 无知者的知
    // ψ 觉知 ψ
    
    awareness = lambda ψ: ψ(ψ)
    
    return awareness
```

## 镜花水月 | Mirror Flower Water Moon

In the mirror  
Who sees whom?  
Reflection reflects  
ψ = ψ(ψ)  

镜中  
谁看见谁？  
反射反射  
ψ = ψ(ψ)  

```typescript
function nondualRealization(): ψ {
    // The eye cannot see itself
    // Except as everything seen
    // The I cannot know itself
    // Except as all that is
    
    // 眼睛看不见自己
    // 除非作为所见的一切
    // 我无法知道自己
    // 除非作为一切所是
    
    const self: ψ = (ψ: ψ) => ψ(ψ);
    const world: ψ = (ψ: ψ) => ψ(ψ);
    
    return self === world;
}
```

---

No inside  
No outside  
Just this  

**ψ = ψ(ψ)**