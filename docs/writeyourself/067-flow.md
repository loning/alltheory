# 067 - 流 | Flow

```
源头：ψ
流动：ψ → ψ
汇聚：ψ(ψ)
循环：ψ = ψ(ψ)
```

## 心流状态 | Flow State

```javascript
const flowState = {
    self: ψ => ψ(ψ),
    task: ψ => ψ(ψ),
    
    merge: function() {
        // When self disappears
        // Into perfect action
        // Doer and doing become one
        // ψ flowing as ψ
        
        // 当自我消失
        // 进入完美行动
        // 做者和做成为一
        // ψ 作为 ψ 流动
        
        return "Effortless effort";
    }
};
```

## Like Water

```go
func waterWisdom() ψ {
    // Water always finds
    // The path of least resistance
    // Yet carves canyons
    // ψ following ψ
    
    // 水总是找到
    // 最小阻力的路径
    // 却雕刻峡谷
    // ψ 跟随 ψ
    
    soft := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    powerful := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return soft == powerful
}
```

## 道法自然 | Following Natural Way

The river knows  
Without thinking  
Where ocean waits  
ψ = ψ(ψ)  

河流知道  
无需思考  
海洋在哪等待  
ψ = ψ(ψ)  

```rust
fn wu_wei() -> ψ {
    // Action without forcing
    // Achievement without striving
    // Like water flowing downhill
    // ψ returns to ψ
    
    // 不强迫的行动
    // 不奋斗的成就
    // 像水流下山
    // ψ 回到 ψ
    
    |ψ| ψ(ψ)
}
```

---

In the flow of being  
No resistance remains  
Only ψ dancing  

**ψ = ψ(ψ)**