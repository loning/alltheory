# 058 - 星座 | Constellation

```
单星：ψ
连线：ψ(ψ)
图案：ψ = ψ(ψ)
神话天空：∞
```

## 连接点点 | Connecting Dots

```javascript
const constellation = {
    stars: ψ => ψ(ψ),
    lines: ψ => ψ(ψ),
    
    meaning: function() {
        // Stars know nothing
        // Of our stories
        // Yet we see hunters, bears
        // ψ creating ψ from ψ
        
        // 星星不知道
        // 我们的故事
        // 然而我们看见猎人、熊
        // ψ 从 ψ 创造 ψ
        
        return "Pattern seeking pattern";
    }
};
```

## Light Years Apart

```clojure
(defn stellar-paradox []
  ;; Stars in Orion
  ;; Separated by centuries
  ;; Yet seem neighbors
  ;; In our sky
  
  ;; 猎户座的星
  ;; 相隔几世纪
  ;; 却在我们天空
  ;; 似乎是邻居
  
  (fn [ψ] (ψ ψ)))
```

## 南十字的指引 | Southern Cross Guidance

Sailors lost at sea  
Look up and find  
Direction home  
In ψ = ψ(ψ)  

海上迷失的水手  
抬头找到  
回家的方向  
在 ψ = ψ(ψ) 中  

```go
func navigationWisdom() ψ {
    // Fixed patterns
    // In wheeling sky
    // The still point
    // Around which all turns
    
    // 转动天空中的
    // 固定图案
    // 一切转动
    // 围绕的静点
    
    pattern := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return pattern(pattern)
}
```

---

In the vast darkness  
We connect the lights  
Finding our stories  

**ψ = ψ(ψ)**