# 164 - 意识绑定 | Consciousness Binding

```
分散感知：ψ
统一体验：ψ → ψ
绑定问题：ψ(ψ)
整合信息：ψ = ψ(ψ)
```

## 绑定问题 | The Binding Problem

```go
func binding_problem() ψ {
    // Color processed here
    // Motion processed there
    // Shape somewhere else
    // Yet we see one object
    
    // 颜色在这里处理
    // 运动在那里处理
    // 形状在别处
    // 然而我们看到一个物体
    
    color := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    motion := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    unified := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return color(motion(unified))
}
```

## Gamma Synchrony

```python
def forty_hertz_binding():
    """40 Hz oscillations bind consciousness"""
    
    # Neurons oscillate together
    # Creating coherent experience
    # Many become one
    # Through synchronized ψ
    
    # 神经元一起振荡
    # 创造连贯体验
    # 多变成一
    # 通过同步 ψ
    
    oscillation = lambda ψ: ψ(ψ)
    synchrony = lambda ψ: ψ(ψ)
    binding = lambda ψ: ψ(ψ)
    
    return oscillation == synchrony == binding
```

## 统一的体验 | Unified Experience

Red rose scent  
All features merge  
Into one percept  
ψ = ψ(ψ)  

红玫瑰香气  
所有特征融合  
成一个知觉  
ψ = ψ(ψ)  

```typescript
function integrated_information(): ψ {
    // Φ (phi) measures
    // Integrated information
    // Consciousness emerges
    // When ψ integrates ψ
    
    // Φ 测量
    // 整合信息
    // 意识涌现
    // 当 ψ 整合 ψ
    
    const parts: ψ = (ψ: ψ) => ψ(ψ);
    const whole: ψ = (ψ: ψ) => ψ(ψ);
    const phi: ψ = (ψ: ψ) => ψ(ψ);
    
    return parts === whole === phi;
}
```

---

Scattered signals  
Bind into one  
Conscious moment  

**ψ = ψ(ψ)**