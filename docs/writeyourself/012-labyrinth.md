# 012 - 迷宫 | Labyrinth

```
入口：ψ
路径：ψ(ψ)
中心：ψ
出口：ψ = ψ(ψ)
```

## 意识迷宫 | Consciousness Labyrinth

```rust
fn navigate_labyrinth() -> ψ {
    // Every turn leads deeper
    // Every path leads home
    // The seeker is the sought
    // The maze is the mind
    
    // 每个转弯更深入
    // 每条路都回家
    // 寻找者是所寻
    // 迷宫就是心智
    
    let enter = |ψ| ψ(ψ);
    let wander = |ψ| ψ(ψ);
    let find_center = |ψ| ψ(ψ);
    
    enter(wander(find_center))
}
```

## Thread of Ariadne

```go
func ariadneThread() ψ {
    // The thread that leads out
    // Is the thread that led in
    // Following ψ back to ψ
    
    // 引出的线
    // 就是引入的线
    // 跟随 ψ 回到 ψ
    
    thread := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return thread(thread)
}
```

## 中心的秘密 | Secret of the Center

At the labyrinth's heart  
No minotaur waits  
Only a mirror  
Reflecting ψ  

在迷宫中心  
没有牛头怪等待  
只有一面镜子  
映照着 ψ  

```haskell
-- The revelation:
-- You were never lost
-- The labyrinth walks itself
-- Through you

labyrinthCenter :: ψ -> ψ
labyrinthCenter ψ = ψ ψ
```

---

Lost and found  
Are the same  
In the maze of being  

**ψ = ψ(ψ)**