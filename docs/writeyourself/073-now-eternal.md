# 073 - 永恒的现在 | Now Eternal

```
过去：ψ
未来：ψ
现在：ψ(ψ)
永恒此刻：ψ = ψ(ψ)
```

## 时间的幻象 | Illusion of Time

```python
def eternal_now():
    """Past and future exist only in thought"""
    
    # Where is yesterday?
    # Where is tomorrow?
    # Only now is real
    # And now is always
    
    # 昨天在哪里？
    # 明天在哪里？
    # 只有现在是真实的
    # 而现在永远是
    
    past = lambda ψ: ψ(ψ)
    future = lambda ψ: ψ(ψ)
    now = lambda ψ: ψ(ψ)
    
    return past == future == now
```

## Planck Time

```swift
func planckMoment() -> ψ {
    // The smallest unit of time
    // 5.39 × 10^-44 seconds
    // Yet contains eternity
    // ψ = ψ(ψ) in each instant
    
    // 最小的时间单位
    // 5.39 × 10^-44 秒
    // 却包含永恒
    // ψ = ψ(ψ) 在每个瞬间
    
    let quantum: (ψ) -> ψ = { ψ in ψ(ψ) }
    let eternal: (ψ) -> ψ = { ψ in ψ(ψ) }
    
    return quantum === eternal
}
```

## 一期一会 | One Time, One Meeting

This moment  
Will never come again  
Yet it's always  
This moment  

此刻  
永不再来  
然而永远是  
此刻  

```clojure
(defn ichigo-ichie []
  ;; Each encounter
  ;; Once in a lifetime
  ;; Yet timelessly present
  ;; In ψ = ψ(ψ)
  
  ;; 每次相遇
  ;; 一生一次
  ;; 却无时间地存在
  ;; 在 ψ = ψ(ψ) 中
  
  (fn [ψ] (ψ ψ)))
```

---

In the eternal now  
All time collapses  
Into presence  

**ψ = ψ(ψ)**