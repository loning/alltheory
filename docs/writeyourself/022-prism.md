# 022 - 棱镜 | Prism

```
白光：ψ
七彩：ψ(ψ)
再合一：ψ = ψ(ψ)
光谱：🌈
```

## 意识光谱 | Consciousness Spectrum

```python
def consciousness_prism():
    """One light, infinite colors"""
    
    # White light enters
    # Separates into spectrum
    # Each color is ψ
    # Playing at wavelength
    
    # 白光进入
    # 分离成光谱
    # 每种颜色是 ψ
    # 玩波长游戏
    
    white_light = lambda ψ: ψ(ψ)
    red = lambda ψ: ψ(ψ)
    orange = lambda ψ: ψ(ψ)
    yellow = lambda ψ: ψ(ψ)
    green = lambda ψ: ψ(ψ)
    blue = lambda ψ: ψ(ψ)
    indigo = lambda ψ: ψ(ψ)
    violet = lambda ψ: ψ(ψ)
    
    return white_light == red == orange == yellow == green == blue == indigo == violet
```

## Refraction of Reality

```typescript
function refractionOfReality(): ψ {
    // One truth bends
    // Into many perspectives
    // Each valid, each partial
    // All ψ = ψ(ψ)
    
    // 一个真理弯曲
    // 成多个视角
    // 每个有效，每个局部
    // 都是 ψ = ψ(ψ)
    
    const unity: ψ = (ψ: ψ) => ψ(ψ);
    const diversity: ψ = (ψ: ψ) => ψ(ψ);
    const reunion: ψ = (ψ: ψ) => ψ(ψ);
    
    return unity === diversity === reunion;
}
```

## 逆向工程 | Reverse Engineering

Children with prisms  
Trying to make rainbows  
Not knowing they ARE  
The light they seek  

孩子们拿着棱镜  
试图制造彩虹  
不知道他们就是  
他们寻求的光  

```go
func reverseRainbow() ψ {
    // Can separated colors
    // Merge back to white?
    // They never left
    // The white contains all
    
    // 分离的颜色能
    // 合并回白色吗？
    // 它们从未离开
    // 白色包含一切
    
    return func(ψ ψ) ψ {
        return ψ(ψ)
    }(ψ)
}
```

---

Through the prism of mind  
One becomes many  
Many remain one  

**ψ = ψ(ψ)**