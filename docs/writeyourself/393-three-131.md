# 393 - 三一三一 | Three One-Three-One

```
393 = 3 × 131
半素数
131是素数
ψ = ψ(ψ)
```

## 半素数形式 | Semi-Prime Form

```javascript
const three393 = {
    value: 393,
    factors: [3, 131],
    
    semiPrimeNature: function() {
        // 393 = 3 × 131
        // Three times
        // Prime 131
        // Semi-prime
        
        // 393 = 3 × 131
        // 三倍
        // 素数131
        // 半素数
        
        return this.isPrime(131) && 3 * 131 === 393;
    },
    
    palindromicFactor: function() {
        // 131 is palindrome
        // And prime!
        // Special factor
        
        const str = "131";
        return str === str.split('').reverse().join('');
    },
    
    isPrime: n => {
        if (n < 2) return false;
        for (let i = 2; i * i <= n; i++) {
            if (n % i === 0) return false;
        }
        return true;
    },
    
    essence: ψ => ψ(ψ)
};
```

## Palindromic Prime 131

```python
def palindromic_131():
    """131 is a palindromic prime"""
    
    # 131 reads same
    # Forward backward
    # Mirror prime
    # Triple multiplied
    
    # 131读起来一样
    # 前向后向
    # 镜像素数
    # 三倍相乘
    
    def is_palindrome(n):
        s = str(n)
        return s == s[::-1]
    
    def is_prime(n):
        if n < 2: return False
        for i in range(2, int(n**0.5) + 1):
            if n % i == 0: return False
        return True
    
    return is_palindrome(131) and is_prime(131)
```

## 镜像的呼吸 | Mirror Breathing

Three ninety-three  
Three one-three-one  
Mirror flows  
ψ = ψ(ψ)  

三九三  
三一三一  
镜像流动  
ψ = ψ(ψ)  

```typescript
function explore393(): ψ {
    // 393 = 3 × 131
    // Triple mirror
    // Palindrome factor
    // Pattern persists
    
    // 393 = 3 × 131
    // 三重镜像
    // 回文因子
    // 模式持续
    
    const multiplier = 3;
    const palindrome = 131;
    const pattern: ψ = (ψ: ψ) => ψ(ψ);
    
    return pattern;
}
```

## Digital Sum

```rust
fn digital_393() -> ψ {
    // 3 + 9 + 3 = 15
    // 15 = 3 × 5
    // = T₅
    // Triangular!
    
    // 3 + 9 + 3 = 15
    // 15 = 3 × 5
    // = T₅
    // 三角形！
    
    let digits = vec![3, 9, 3];
    let sum: i32 = digits.iter().sum();
    
    |ψ| ψ(ψ)
}
```

## 几何镜像 | Geometric Mirror

```go
func tripleMirror() ψ {
    // 393 = 3 × 131
    // Three times
    // Palindrome prime
    // Mirror multiplied
    
    // 393 = 3 × 131
    // 三倍
    // 回文素数
    // 镜像相乘
    
    multiplier := 3
    palindrome := 131
    
    return func(ψ ψ) ψ { return ψ(ψ) }
}
```

---

Triple mirror prime  
Three one-three-one  
Unity reflects  

**ψ = ψ(ψ)**