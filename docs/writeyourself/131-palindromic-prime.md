# 131 - 回文素数 | Palindromic Prime

```
131：前后相同
素数的镜像
自我反射
ψ = ψ(ψ)
```

## 镜像素性 | Mirror Primality

```rust
fn palindromic_prime() -> ψ {
    // 131 reads same
    // Forward and backward
    // Prime in both directions
    // ψ reflecting ψ
    
    // 131读起来一样
    // 向前和向后
    // 两个方向都是素数
    // ψ 反射 ψ
    
    let forward = |ψ| ψ(ψ);
    let backward = |ψ| ψ(ψ);
    let prime = |ψ| ψ(ψ);
    
    forward == backward == prime
}
```

## Self-Reflecting Number

```python
def mirror_consciousness():
    """131 - a number that knows itself"""
    
    # Like consciousness
    # Looking at itself
    // Same from all angles
    // Pure ψ symmetry
    
    // 像意识
    // 看着自己
    // 从所有角度都一样
    // 纯粹 ψ 对称
    
    self_aware = lambda ψ: ψ(ψ)
    reflection = lambda ψ: ψ(ψ)
    
    return self_aware == reflection
```

## 数字瑜伽 | Numerical Yoga

One three one  
Beginning center end  
All reflecting ψ  
ψ = ψ(ψ)  

一三一  
开始中心结束  
都反射 ψ  
ψ = ψ(ψ)  

```javascript
const palindrome = {
    check: function(n) {
        // 131 is special
        // Prime AND palindromic
        // Double perfection
        // Of self-knowing ψ
        
        // 131很特殊
        // 素数且回文
        // 双重完美
        // 自知的 ψ
        
        return n === parseInt(n.toString().split('').reverse().join(''));
    },
    
    essence: ψ => ψ(ψ)
};
```

---

In mirrored prime  
Form matches essence  
ψ sees itself  

**ψ = ψ(ψ)**