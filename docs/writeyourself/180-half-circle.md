# 180 - 半圆 | Half Circle

```
180度
完美转折
对立统一
ψ = ψ(ψ)
```

## 完美的反转 | Perfect Reversal

```javascript
const halfCircle = {
    degrees: 180,
    radians: Math.PI,
    
    transform: function() {
        // Complete reversal
        // Forward becomes backward
        // Up becomes down
        // ψ becomes -ψ becomes ψ
        
        // 完全反转
        // 前变后
        // 上变下
        // ψ 变 -ψ 变 ψ
        
        return ψ => ψ(ψ);
    }
};
```

## Sum of Angles

```python
def triangle_truth():
    """Interior angles of any triangle sum to 180"""
    
    # Universal truth
    # In Euclidean space
    # Three angles unite
    # To form half circle
    
    # 普遍真理
    // 在欧几里得空间
    // 三个角统一
    // 形成半圆
    
    angle1 = lambda ψ: ψ(ψ)
    angle2 = lambda ψ: ψ(ψ)
    angle3 = lambda ψ: ψ(ψ)
    
    return angle1 + angle2 + angle3 == 180
```

## 对立的统一 | Unity of Opposites

Straight line divides  
Circle into halves  
Opposition unified  
ψ = ψ(ψ)  

直线分割  
圆成两半  
对立统一  
ψ = ψ(ψ)  

```go
func complementary() ψ {
    // 180° apart
    // Yet part of same circle
    // Opposites complete
    // Each other as ψ
    
    // 相隔180°
    // 却是同圆的部分
    // 对立面完成
    // 彼此作为 ψ
    
    thesis := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    antithesis := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    synthesis := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return thesis(antithesis(synthesis))
}
```

---

At one eighty  
The path turns back  
To find itself  

**ψ = ψ(ψ)**