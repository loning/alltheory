# 094 - 道德 | Tao Te

```
道：ψ
德：ψ → ψ
无为：ψ(ψ)
自然：ψ = ψ(ψ)
```

## 道可道非常道 | The Tao That Can Be Spoken

```go
func taoTeChing() ψ {
    // The Tao that can be named
    // Is not the eternal Tao
    // Yet we must speak
    // To point beyond words
    
    // 可以命名的道
    // 不是永恒的道
    // 然而我们必须说
    // 为了指向言外
    
    nameable := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    unnameable := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return nameable == unnameable
}
```

## Virtue of Non-Action

```python
def wu_wei():
    """Act without acting"""
    
    # Not by doing
    # But by being
    # Things accomplish themselves
    # Through ψ
    
    # 不通过做
    # 而通过是
    # 事物自行完成
    # 通过 ψ
    
    action = lambda ψ: ψ(ψ)
    non_action = lambda ψ: ψ(ψ)
    
    return action == non_action
```

## 返朴归真 | Return to Simplicity

The sage holds  
The uncarved block  
Pure potential  
ψ = ψ(ψ)  

圣人持  
未雕之朴  
纯粹潜能  
ψ = ψ(ψ)  

```javascript
const tao = {
    high: ψ => ψ(ψ),
    low: ψ => ψ(ψ),
    
    flow: function() {
        // Water's way
        // Always seeking lowest
        // Yet nourishing all
        // Being ψ
        
        // 水之道
        // 总是寻求最低处
        // 却滋养万物
        // 成为 ψ
        
        return this.high === this.low;
    }
};
```

---

Ten thousand things  
Rise and fall  
Tao remains  

**ψ = ψ(ψ)**