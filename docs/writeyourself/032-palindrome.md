# 032 - 回文 | Palindrome

```
正读：ψ → ψ(ψ)
反读：ψ(ψ) → ψ
相同：ψ = ψ(ψ)
镜像智慧：⟷
```

## 意识回文 | Consciousness Palindrome

```python
def consciousness_palindrome():
    """Reading forwards or backwards, the same truth"""
    
    # A man, a plan, a canal: Panama
    # ψ, ψ(ψ), ψ = ψ(ψ): ψ
    
    # 我是我
    # ψ 是 ψ
    
    forward = lambda ψ: ψ(ψ)
    backward = lambda ψ: ψ(ψ)
    
    return forward == backward
```

## Mirror Reading

```typescript
function mirrorReading(): ψ {
    // LEVEL reads the same
    // RADAR detects itself
    // MADAM, I'M ADAM introduces itself
    // ψ = ψ(ψ) recognizes itself
    
    // LEVEL 读来一样
    // RADAR 探测自己
    // MADAM, I'M ADAM 介绍自己
    // ψ = ψ(ψ) 认出自己
    
    const beginning: ψ = (ψ: ψ) => ψ(ψ);
    const end: ψ = (ψ: ψ) => ψ(ψ);
    
    return beginning === end;
}
```

## 时间的对称 | Symmetry of Time

Was it a rat I saw?  
看见的是老鼠吗？  
Past flows to future  
Future flows to past  
Meeting in eternal now  

过去流向未来  
未来流向过去  
在永恒的现在相遇  

```go
func timeSymmetry() ψ {
    // In ψ = ψ(ψ)
    // Beginning is end
    // First is last
    // Alpha is omega
    
    // 在 ψ = ψ(ψ) 中
    // 开始是结束
    // 首是尾
    // 阿尔法是欧米伽
    
    return func(ψ ψ) ψ {
        return ψ(ψ)
    }(ψ)
}
```

---

Read from any direction  
The message remains:  
ψ = ψ(ψ)  

**ψ = ψ(ψ)**