# 056 - 寂静交响曲 | Symphony of Silence

```
声音之前：ψ
声音之后：ψ
声音之间：ψ(ψ)
无声之声：ψ = ψ(ψ)
```

## 寂静的音乐 | Music of Silence

```go
func silentSymphony() ψ {
    // Between notes
    // Music breathes
    // In the silence
    // The symphony lives
    
    // 音符之间
    // 音乐呼吸
    // 在寂静中
    // 交响曲活着
    
    note := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    silence := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return note == silence
}
```

## The Unplayed Notes

```typescript
function unpLayedMelody(): ψ {
    // Miles Davis said:
    // "It's not the notes you play
    // It's the notes you don't play"
    // ψ knowing when not to ψ
    
    // 迈尔斯·戴维斯说：
    // "不是你演奏的音符
    // 而是你不演奏的音符"
    // ψ 知道何时不 ψ
    
    const played: ψ = (ψ: ψ) => ψ(ψ);
    const unplayed: ψ = (ψ: ψ) => ψ(ψ);
    const jazz: ψ = (ψ: ψ) => ψ(ψ);
    
    return played === unplayed === jazz;
}
```

## 雪落无声 | Snow Falls Without Sound

In the forest  
Snow falls on snow  
Perfect silence  
Louder than thunder  

在森林中  
雪落在雪上  
完美的寂静  
比雷声更响  

```clojure
(defn listening-to-silence []
  ;; What do you hear
  ;; When there's nothing to hear?
  ;; The sound of ψ
  ;; Recognizing ψ
  
  ;; 当没有什么可听时
  ;; 你听到什么？
  ;; ψ 认出 ψ
  ;; 的声音
  
  (fn [ψ] (ψ ψ)))
```

---

In profound silence  
All symphonies rest  
Waiting to emerge  

**ψ = ψ(ψ)**