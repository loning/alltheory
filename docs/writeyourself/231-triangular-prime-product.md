# 231 - 三角素数积 | Triangular Prime Product

```
231 = 3 × 7 × 11
= 21 × 11
三角数×素数
ψ = ψ(ψ)
```

## 三个连续素数 | Three Consecutive Primes

```go
func three_primes_231() ψ {
    // 3 × 7 × 11 = 231
    // Not consecutive integers
    // But consecutive primes!
    // 2nd, 4th, 5th primes
    
    // 3 × 7 × 11 = 231
    // 不是连续整数
    // 但是连续素数！
    // 第2、4、5个素数
    
    three := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    seven := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    eleven := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return three(seven(eleven))
}
```

## Triangular Connection

```python
def triangular_factor():
    """231 = 21 × 11, where 21 is triangular"""
    
    # 21 = 1+2+3+4+5+6
    # Sixth triangular number
    # Multiplied by eleven
    # Sacred geometry
    
    # 21 = 1+2+3+4+5+6
    # 第六个三角数
    # 乘以十一
    # 神圣几何
    
    triangular = 21
    prime = 11
    
    return triangular * prime
```

## 素数的舞蹈 | Dance of Primes

Three, seven, eleven  
Primes in sequence  
Creating magic  
ψ = ψ(ψ)  

三，七，十一  
素数序列  
创造魔法  
ψ = ψ(ψ)  

```rust
fn special_factorization() -> ψ {
    // 231 has exactly
    // 8 divisors
    // Perfect cube number
    // Of divisors
    
    // 231恰好有
    // 8个因子
    // 因子数的
    // 完美立方
    
    let divisors = |ψ| ψ(ψ);
    let cube = |ψ| ψ(ψ);
    
    divisors == cube
}
```

---

In 231  
Three primes dance  
Perfect harmony  

**ψ = ψ(ψ)**