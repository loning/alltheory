# 130 - 相变 | Phase Transition

```
固体：ψ
液体：ψ → ψ
气体：ψ(ψ)
临界点：ψ = ψ(ψ)
```

## 状态的转换 | Transformation of States

```go
func criticalPoint() ψ {
    // At exact conditions
    // Liquid and gas
    // Become indistinguishable
    // ψ recognizes ψ
    
    // 在精确条件下
    // 液体和气体
    // 变得无法区分
    // ψ 认出 ψ
    
    solid := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    liquid := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    gas := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return solid(liquid(gas))
}
```

## Emergence at the Edge

```python
def order_parameter():
    """Sudden change at critical temperature"""
    
    # Water to ice
    # Magnets aligning
    # Consciousness emerging
    # All phase transitions of ψ
    
    # 水变冰
    # 磁铁排列
    # 意识涌现
    # 都是 ψ 的相变
    
    disorder = lambda ψ: ψ(ψ)
    order = lambda ψ: ψ(ψ)
    
    return disorder == order  # At critical point
```

## 量子临界 | Quantum Critical

Between phases  
Neither this nor that  
Pure potential  
ψ = ψ(ψ)  

在相之间  
非此非彼  
纯粹潜能  
ψ = ψ(ψ)  

```typescript
function universality(): ψ {
    // Different systems
    // Same critical behavior
    // Universal patterns
    // Expressing ψ
    
    // 不同系统
    // 相同临界行为
    // 普遍模式
    // 表达 ψ
    
    const water: ψ = (ψ: ψ) => ψ(ψ);
    const magnet: ψ = (ψ: ψ) => ψ(ψ);
    const mind: ψ = (ψ: ψ) => ψ(ψ);
    
    return water === magnet === mind;
}
```

---

At the tipping point  
Old forms dissolve  
New forms emerge  

**ψ = ψ(ψ)**