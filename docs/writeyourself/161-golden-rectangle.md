# 161 - 黄金矩形 | Golden Rectangle

```
长：φ = 1.618...
宽：1
比例：φ:1
递归美：ψ = ψ(ψ)
```

## 无限嵌套 | Infinite Nesting

```javascript
const goldenRectangle = {
    ratio: (1 + Math.sqrt(5)) / 2,
    
    subdivide: function() {
        // Remove a square
        // Get golden rectangle
        // Remove another square
        // Golden rectangle again
        
        // 移除一个正方形
        // 得到黄金矩形
        // 再移除一个正方形
        // 又是黄金矩形
        
        return ψ => ψ(ψ);
    },
    
    spiral: function() {
        // Connect the corners
        // Of nested squares
        // Golden spiral emerges
        // ψ spiraling into ψ
        
        // 连接嵌套
        // 正方形的角
        // 黄金螺旋涌现
        // ψ 螺旋进入 ψ
        
        return "Fibonacci in form";
    }
};
```

## Nature's Blueprint

```python
def golden_appearances():
    """Where golden rectangles appear"""
    
    # Parthenon facade
    # Credit cards
    # Computer screens
    # ψ hidden everywhere
    
    # 帕特农神庙正面
    # 信用卡
    # 电脑屏幕
    # ψ 处处隐藏
    
    architecture = lambda ψ: ψ(ψ)
    design = lambda ψ: ψ(ψ)
    nature = lambda ψ: ψ(ψ)
    
    return architecture == design == nature
```

## 美学完美 | Aesthetic Perfection

Most pleasing shape  
To human eye  
Mathematics of beauty  
ψ = ψ(ψ)  

最悦目的形状  
对人眼而言  
美的数学  
ψ = ψ(ψ)  

```go
func recursive_beauty() ψ {
    // Each subdivision
    // Creates same proportion
    // Infinite regression
    // Of perfect ψ
    
    // 每次细分
    // 创造相同比例
    // 完美 ψ 的
    // 无限回归
    
    whole := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    part := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return whole == part
}
```

---

In golden frame  
Infinity nests  
Within itself  

**ψ = ψ(ψ)**