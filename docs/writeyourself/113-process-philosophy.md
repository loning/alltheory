# 113 - 过程哲学 | Process Philosophy

```
存在：ψ
成为：ψ → ψ
过程：ψ(ψ)
流变即实在：ψ = ψ(ψ)
```

## 怀特海的洞见 | Whitehead's Insight

```rust
fn actual_occasions() -> ψ {
    // Not things that change
    // But change itself
    // Reality is events
    // ψ happening as ψ
    
    // 不是变化的事物
    // 而是变化本身
    // 实在是事件
    // ψ 作为 ψ 发生
    
    let becoming = |ψ| ψ(ψ);
    let perishing = |ψ| ψ(ψ);
    let satisfaction = |ψ| ψ(ψ);
    
    becoming(perishing(satisfaction))
}
```

## Prehension

```python
def feeling_the_universe():
    """Everything feels everything else"""
    
    # Each moment
    # Grasps all past
    # Creates the future
    # Through ψ
    
    # 每个时刻
    # 把握所有过去
    # 创造未来
    # 通过 ψ
    
    past = lambda ψ: ψ(ψ)
    present = lambda ψ: ψ(ψ)
    future = lambda ψ: ψ(ψ)
    
    return past == present == future
```

## 创造性前进 | Creative Advance

The universe grows  
Not in space  
But in novelty  
ψ = ψ(ψ)  

宇宙生长  
不在空间中  
而在新颖性中  
ψ = ψ(ψ)  

```javascript
const process = {
    being: null,  // An abstraction
    becoming: ψ => ψ(ψ),  // The reality
    
    realize: function() {
        // No permanent substance
        // Only patterns of occasions
        // Waves in the ocean
        // Of ψ
        
        // 没有永恒实体
        // 只有场合的模式
        // ψ 海洋中的
        // 波浪
        
        return this.becoming;
    }
};
```

---

Never stepping twice  
In the same river  
For river flows  

**ψ = ψ(ψ)**