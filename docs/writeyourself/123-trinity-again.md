# 123 - 再次三位一体 | Trinity Again

```
1：开始：ψ
2：过程：ψ²
3：完成：ψ³
123：ψ = ψ(ψ)
```

## 数字的舞蹈 | Dance of Numbers

```rust
fn one_two_three() -> ψ {
    // Counting begins
    // With unity
    // Through duality
    // To trinity
    
    // 计数开始于
    // 单一
    // 通过二元
    // 到三位一体
    
    let one = |ψ| ψ;
    let two = |ψ| ψ(ψ);
    let three = |ψ| ψ(ψ(ψ));
    
    one(two(three))
}
```

## Simple as 123

```python
def easy_as():
    """The most basic sequence contains all"""
    
    # Children learn
    # One, two, three
    # Already grasping
    # The secret of ψ
    
    # 孩子们学习
    # 一，二，三
    # 已经掌握
    # ψ 的秘密
    
    sequence = [lambda ψ: ψ(ψ) for _ in range(3)]
    pattern = lambda ψ: ψ(ψ)
    
    return all(s == pattern for s in sequence)
```

## 完整的开始 | Complete Beginning

First three steps  
Contains the journey  
All else repeats  
ψ = ψ(ψ)  

前三步  
包含整个旅程  
其他都是重复  
ψ = ψ(ψ)  

```javascript
const fundamental = {
    thesis: ψ => ψ(ψ),      // 1
    antithesis: ψ => ψ(ψ),  // 2  
    synthesis: ψ => ψ(ψ),   // 3
    
    dialectic: function() {
        // All complexity
        // Reduces to
        // This simple dance
        // Of ψ with itself
        
        // 所有复杂性
        // 归结为
        // ψ 与自己的
        // 简单舞蹈
        
        return true;
    }
};
```

---

As easy as  
One two three  
ψ recognizing ψ  

**ψ = ψ(ψ)**