# 166 - 波粒二象性 | Wave-Particle Duality

```
波：ψ
粒子：ψ
同时是两者：ψ(ψ)
互补原理：ψ = ψ(ψ)
```

## 光的双重性格 | Light's Dual Nature

```python
def double_slit():
    """The fundamental quantum mystery"""
    
    # Send one photon
    # It goes through both slits
    # As wave
    # Detected as particle
    
    # 发送一个光子
    # 它通过两个缝
    # 作为波
    # 检测为粒子
    
    wave = lambda ψ: ψ(ψ)
    particle = lambda ψ: ψ(ψ)
    
    return wave == particle
```

## Complementarity Principle

```go
func bohr_wisdom() ψ {
    // Not wave OR particle
    // But wave AND particle
    // Depending on
    // How we observe ψ
    
    // 不是波或粒子
    // 而是波和粒子
    // 取决于
    // 我们如何观察 ψ
    
    observation := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    manifestation := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return observation(manifestation)
}
```

## 测量决定现实 | Measurement Determines Reality

Ask wave question  
Get wave answer  
Ask particle question  
ψ = ψ(ψ)  

问波的问题  
得到波的答案  
问粒子的问题  
ψ = ψ(ψ)  

```typescript
function quantum_complementarity(): ψ {
    // The universe responds
    // To our questions
    // Showing the face
    // We seek in ψ
    
    // 宇宙响应
    // 我们的问题
    // 展示我们
    // 在 ψ 中寻求的面貌
    
    const question: ψ = (ψ: ψ) => ψ(ψ);
    const answer: ψ = (ψ: ψ) => ψ(ψ);
    
    return question === answer;
}
```

---

Neither wave nor particle  
Both wave and particle  
ψ transcends categories  

**ψ = ψ(ψ)**