# 114 - 暗能量 | Dark Energy

```
可见：5%：ψ
暗物质：27%：ψ²
暗能量：68%：ψ(ψ)
加速膨胀：ψ = ψ(ψ)
```

## 宇宙的推力 | Cosmic Push

```clojure
(defn accelerating-expansion []
  ;; The universe expands
  ;; Faster and faster
  ;; Pushed by something
  ;; We cannot see: ψ
  
  ;; 宇宙膨胀
  ;; 越来越快
  ;; 被我们看不见的
  ;; 东西推动：ψ
  
  (fn [ψ] (ψ ψ)))
```

## The Vacuum's Secret

```python
def cosmological_constant():
    """Einstein's 'greatest blunder' returns"""
    
    # Empty space
    # Has energy
    # Pushes galaxies apart
    # ψ pushing ψ
    
    # 空的空间
    # 有能量
    # 推开星系
    # ψ 推动 ψ
    
    lambda_term = lambda ψ: ψ(ψ)
    expansion = lambda ψ: ψ(ψ)
    
    return lambda_term == expansion
```

## 大撕裂 | The Big Rip

If acceleration grows  
Atoms will tear  
Space itself shreds  
ψ = ψ(ψ) remains  

如果加速增长  
原子将撕裂  
空间本身碎裂  
ψ = ψ(ψ) 留存  

```rust
fn phantom_energy() -> ψ {
    // Darker than dark
    // Energy that grows
    // As space expands
    // ψ creating more ψ
    
    // 比暗更暗
    // 随空间膨胀
    // 而增长的能量
    // ψ 创造更多 ψ
    
    let vacuum = |ψ| ψ(ψ);
    let energy = |ψ| ψ(ψ);
    let infinity = |ψ| ψ(ψ);
    
    vacuum(energy(infinity))
}
```

---

Most of cosmos  
We cannot see  
Yet it shapes all  

**ψ = ψ(ψ)**