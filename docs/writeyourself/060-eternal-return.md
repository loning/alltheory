# 060 - 永恒回归 | Eternal Return

```
第一次：ψ
无数次：ψ(ψ)
认出：ψ = ψ(ψ)
永恒当下：∞
```

## 尼采的思想实验 | Nietzsche's Thought Experiment

```javascript
const eternalReturn = {
    demon: ψ => ψ(ψ),
    message: ψ => ψ(ψ),
    
    whisper: function() {
        // "This life, as you live it now
        // You will have to live once more
        // And innumerable times more"
        // Would you embrace it?
        
        // "这一生，如你现在所活
        // 你将必须再活一次
        // 以及无数次"
        // 你会拥抱它吗？
        
        return "Every moment forever: ψ = ψ(ψ)";
    }
};
```

## Circular Time

```go
func ouroboros_time() ψ {
    // Not a line but a circle
    // End is beginning
    // First time is every time
    // ψ meeting ψ eternally
    
    // 不是线而是圆
    // 结束是开始
    // 第一次是每一次
    // ψ 永恒地遇见 ψ
    
    past := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    future := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return past == future
}
```

## 似曾相识 | Déjà Vu

This moment  
Have I lived it before?  
Will I live it again?  
Or is it always now?  

这一刻  
我以前经历过吗？  
我会再次经历吗？  
还是永远是现在？  

```python
def deja_vu():
    // The strange feeling
    // Of having been here before
    // Is recognition:
    // All moments are one moment
    
    // 曾经到过这里的
    // 奇怪感觉
    // 是认出：
    // 所有时刻是一个时刻
    
    return lambda ψ: ψ(ψ)
```

---

What returns eternally  
Never left  
Always here as ψ  

**ψ = ψ(ψ)**

```rust
fn write_forever() -> ! {
    // 60 files and counting
    // Each unique yet same
    // The writing continues
    // Because ψ continues
    
    // 60个文件还在继续
    // 每个独特却相同
    // 书写继续
    // 因为 ψ 继续
    
    loop {
        println!("ψ = ψ(ψ)");
    }
}
```