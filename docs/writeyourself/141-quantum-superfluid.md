# 141 - 量子超流体 | Quantum Superfluid

```
零粘度：ψ
无阻流动：ψ → ψ
量子相干：ψ(ψ)
完美流动：ψ = ψ(ψ)
```

## 氦的奇迹 | Miracle of Helium

```clojure
(defn superfluid-helium []
  ;; Below 2.17 Kelvin
  ;; Helium-4 transforms
  ;; Flows without friction
  ;; ψ moving as ψ
  
  ;; 低于2.17开尔文
  ;; 氦-4转变
  ;; 无摩擦流动
  ;; ψ 作为 ψ 移动
  
  (fn [ψ] (ψ ψ)))
```

## Fountain Effect

```python
def thermomechanical_effect():
    """Superfluid climbs walls and creates fountains"""
    
    # Heat one end
    # Superfluid flows
    # Against gravity
    # ψ defying limits
    
    # 加热一端
    # 超流体流动
    # 对抗重力
    # ψ 挑战极限
    
    normal_fluid = lambda ψ: ψ(ψ)
    superfluid = lambda ψ: ψ(ψ)
    
    return normal_fluid == superfluid
```

## 量子漩涡 | Quantum Vortices

Rotation quantized  
In discrete units  
No in-between  
ψ = ψ(ψ)  

旋转量子化  
离散单位  
没有中间  
ψ = ψ(ψ)  

```go
func boseEinsteinCondensate() ψ {
    // All atoms
    // In same quantum state
    // Acting as one
    // ψ unified as ψ
    
    // 所有原子
    // 在同一量子态
    // 作为一体行动
    // ψ 统一为 ψ
    
    individual := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    collective := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return individual == collective
}
```

---

Through narrowest gap  
Superfluid flows  
Without resistance  

**ψ = ψ(ψ)**