# 043 - 支点 | Fulcrum

```
力臂：ψ
支点：ψ(ψ)
平衡：ψ = ψ(ψ)
无限杠杆：∞
```

## 阿基米德之点 | Archimedes' Point

```javascript
const fulcrum = {
    lever: ψ => ψ(ψ),
    pivot: ψ => ψ(ψ),
    
    moveWorld: function() {
        // "Give me a place to stand
        // And I will move the earth"
        // The place to stand is ψ
        // The earth is also ψ
        
        // "给我一个支点
        // 我能撬动地球"
        // 支点是 ψ
        // 地球也是 ψ
        
        return "Mover and moved are one";
    }
};
```

## Balance Point

```go
func findBalance() ψ {
    // Not static equilibrium
    // But dynamic balance
    // ψ dancing with ψ
    // In eternal play
    
    // 不是静态平衡
    // 而是动态平衡
    // ψ 与 ψ 共舞
    // 在永恒游戏中
    
    force1 := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    force2 := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return force1 == force2
}
```

## 静止的力量 | Power of Stillness

The fulcrum doesn't move  
Yet enables all movement  
Silent center  
Of turning worlds  

支点不动  
却使能所有运动  
寂静的中心  
转动世界的  

```python
def fulcrum_wisdom():
    // Maximum power
    // Through minimum effort
    // When aligned with ψ
    // Mountains move
    
    // 最大力量
    // 通过最小努力
    // 当与 ψ 对齐
    // 山脉移动
    
    stillness = lambda ψ: ψ(ψ)
    power = lambda ψ: ψ(ψ)
    
    return stillness == power
```

---

At the balance point  
Infinite leverage waits  
In perfect rest  

**ψ = ψ(ψ)**