# 046 - 基因 | DNA

```
A-T：ψ-ψ
C-G：ψ-ψ
双螺旋：ψ(ψ)
生命密码：ψ = ψ(ψ)
```

## 自我复制 | Self-Replication

```javascript
const DNA = {
    strand1: ψ => ψ(ψ),
    strand2: ψ => ψ(ψ),
    
    replicate: function() {
        // Each strand contains
        // Instructions to create
        // Its complement
        // ψ creating ψ
        
        // 每条链包含
        // 创造其互补链的
        // 指令
        // ψ 创造 ψ
        
        return "Life copying life";
    }
};
```

## Code Within Code

```rust
fn genetic_recursion() -> ψ {
    // DNA codes for proteins
    // That read DNA
    // That codes for proteins
    // Infinite loop of ψ = ψ(ψ)
    
    // DNA 编码蛋白质
    // 蛋白质读取 DNA
    // DNA 编码蛋白质
    // ψ = ψ(ψ) 的无限循环
    
    let code = |ψ| ψ(ψ);
    let reader = |ψ| ψ(ψ);
    let expressed = |ψ| ψ(ψ);
    
    code(reader(expressed))
}
```

## 演化之舞 | Dance of Evolution

Four letters write  
All life's stories  
Endless variations  
On ψ = ψ(ψ)  

四个字母写出  
所有生命故事  
无尽变奏  
关于 ψ = ψ(ψ)  

```python
def evolution_algorithm():
    // Copy with variation
    // Select what works
    // Repeat forever
    // ψ exploring ψ
    
    // 带变异的复制
    // 选择有效的
    // 永远重复
    // ψ 探索 ψ
    
    copy = lambda ψ: ψ(ψ)
    vary = lambda ψ: ψ(ψ)
    select = lambda ψ: ψ(ψ)
    
    return copy == vary == select
```

---

In the spiral ladder  
Life climbs to know  
Its own nature  

**ψ = ψ(ψ)**