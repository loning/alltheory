# 158 - 信息悖论 | Information Paradox

```
黑洞吞噬：ψ
信息消失？：ψ → ?
霍金辐射：?(ψ)
信息守恒：ψ = ψ(ψ)
```

## 黑洞的秘密 | Black Hole's Secret

```clojure
(defn information-paradox []
  ;; Information falls in
  ;; Black hole evaporates
  ;; Where did info go?
  ;; ψ preserves ψ
  
  ;; 信息落入
  ;; 黑洞蒸发
  ;; 信息去哪了？
  ;; ψ 保存 ψ
  
  (fn [ψ] (ψ ψ)))
```

## Holographic Resolution

```python
def holographic_principle():
    """Information encoded on event horizon"""
    
    # Not lost inside
    # But encoded on surface
    # 3D becomes 2D
    # ψ preserving ψ
    
    # 不是丢失在内部
    # 而是编码在表面
    # 3D变成2D
    # ψ 保存 ψ
    
    volume = lambda ψ: ψ(ψ)
    surface = lambda ψ: ψ(ψ)
    
    return volume == surface
```

## 量子纠错 | Quantum Error Correction

Information scrambled  
Not destroyed  
Quantum mechanics  
ψ = ψ(ψ)  

信息被打乱  
不被毁灭  
量子力学  
ψ = ψ(ψ)  

```rust
fn information_conservation() -> ψ {
    // Like energy
    // Information cannot
    // Be created or destroyed
    // Only transformed as ψ
    
    // 像能量
    // 信息不能
    // 被创造或毁灭
    // 只能转化为 ψ
    
    let create = |ψ| ψ(ψ);
    let destroy = |ψ| ψ(ψ);
    let transform = |ψ| ψ(ψ);
    
    create == destroy == transform
}
```

---

In the paradox  
Information finds  
Its way home  

**ψ = ψ(ψ)**