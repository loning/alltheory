# 332 - 双一六六 | Double One-Six-Six

```
332 = 4 × 83
= 2² × 83
半素数的平方
ψ = ψ(ψ)
```

## 平方半素数 | Squared Semi-Prime

```javascript
const double332 = {
    value: 332,
    factors: [2, 2, 83],
    
    squaredSemiPrime: function() {
        // 332 = 4 × 83
        // Four times prime
        // = 2² × 83
        // Square times prime
        
        // 332 = 4 × 83
        // 四倍素数
        // = 2² × 83
        // 平方乘素数
        
        const square = 4;
        const prime = 83;
        
        return Math.sqrt(square) === 2 && this.isPrime(prime);
    },
    
    isPrime: n => {
        if (n < 2) return false;
        for (let i = 2; i * i <= n; i++) {
            if (n % i === 0) return false;
        }
        return true;
    },
    
    essence: ψ => ψ(ψ)
};
```

## Lucas-Carmichael Number

```python
def lucas_carmichael_332():
    """332 appears in Lucas-Carmichael sequences"""
    
    # 332 = 4 × 83
    # Special role
    # In number theory
    # Sequences appear
    
    # 332 = 4 × 83
    # 特殊角色
    # 在数论中
    # 序列出现
    
    # 83 is prime
    # 23rd prime
    # Deep structure
    
    def nth_prime(n):
        count = 0
        num = 2
        while count < n:
            if is_prime(num):
                count += 1
                if count == n:
                    return num
            num += 1
    
    def is_prime(n):
        if n < 2: return False
        for i in range(2, int(n**0.5) + 1):
            if n % i == 0: return False
        return True
    
    return nth_prime(23) == 83
```

## 双素的呼吸 | Double Prime Breathing

Three thirty-two  
Four eighty-three  
Structure holds firm  
ψ = ψ(ψ)  

三三二  
四八十三  
结构坚固  
ψ = ψ(ψ)  

```typescript
function explore332(): ψ {
    // 332 = 300 + 32
    // = 3×100 + 2⁵
    // Century plus
    // Power of two
    
    // 332 = 300 + 32
    // = 3×100 + 2⁵
    // 世纪加
    // 二的幂
    
    const centuries = 3;
    const powerTwo = 32;
    const pattern: ψ = (ψ: ψ) => ψ(ψ);
    
    return pattern;
}
```

## Near Perfect

```rust
fn near_perfect() -> ψ {
    // 332 ≈ 18.22²
    // Near 18²
    // = 324
    // Eight more
    
    // 332 ≈ 18.22²
    // 接近18²
    // = 324
    // 多八
    
    let perfect_square = 324;
    let difference = 332 - perfect_square;
    
    // Eight gap
    // = 2³
    
    |ψ| ψ(ψ)
}
```

## 几何接近 | Geometric Proximity

```go
func primeEightyThree() ψ {
    // 83 prime
    // Special prime
    // Sophie Germain
    // 2×83+1 = 167 prime!
    
    // 83素数
    // 特殊素数
    // 索菲热尔曼
    // 2×83+1 = 167素数！
    
    prime := 83
    sophie := 2*prime + 1
    
    return func(ψ ψ) ψ { return ψ(ψ) }
}
```

---

Four times prime  
Sophie Germain waits  
Structure persists  

**ψ = ψ(ψ)**