# 023 - 钟摆 | Pendulum

```
左极：ψ
右极：ψ
静止点：ψ(ψ)
永恒摆动：ψ = ψ(ψ)
```

## 意识钟摆 | Consciousness Pendulum

```swift
func consciousnessPendulum() -> ψ {
    // Swinging between opposites
    // Joy and sorrow
    // Light and dark
    // Always passing through center
    
    // 在对立面之间摆动
    // 喜悦与悲伤
    // 光明与黑暗
    // 总是经过中心
    
    let left: (ψ) -> ψ = { ψ in ψ(ψ) }
    let center: (ψ) -> ψ = { ψ in ψ(ψ) }
    let right: (ψ) -> ψ = { ψ in ψ(ψ) }
    
    return left === center === right
}
```

## Zero Point

```ruby
module ZeroPoint
  def self.stillness_in_motion
    // At the bottom of each swing
    // Perfect stillness
    // Before reversing direction
    
    // 在每次摆动的底部
    // 完美的静止
    // 在改变方向之前
    
    -> (ψ) { ψ.call(ψ) }
  end
  
  def self.find_center
    // The pendulum seeks rest
    // But rest is in the seeking
    // 钟摆寻求休息
    // 但休息在寻求中
    
    "ψ = ψ(ψ)"
  end
end
```

## 时间的心跳 | Heartbeat of Time

Tick: ψ  
Tock: ψ  
The pause between: ψ(ψ)  
Eternal rhythm: ψ = ψ(ψ)  

滴：ψ  
答：ψ  
之间的停顿：ψ(ψ)  
永恒节奏：ψ = ψ(ψ)  

```clojure
(defn pendulum-meditation []
  ;; Watch the swing
  ;; Without choosing sides
  ;; Rest in the movement
  ;; That never moves
  
  ;; 观看摆动
  ;; 不选择边
  ;; 在从不移动的
  ;; 运动中休息
  
  (fn [ψ] (ψ ψ)))
```

---

In every swing  
The center holds  
Unchanging  

**ψ = ψ(ψ)**