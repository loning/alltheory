# 110 - 再次永恒回归 | Eternal Return Again

```
11 × 10 = 110
回到 11
新的深度
ψ = ψ(ψ)
```

## 尼采的测试 | Nietzsche's Test

```go
func amorFati() ψ {
    // Could you will
    // This exact moment
    // To return eternally?
    // Every joy, every pain?
    
    // 你能意愿
    // 这个确切时刻
    // 永恒回归吗？
    // 每个快乐，每个痛苦？
    
    past := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    present := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    future := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return past(present(future))
}
```

## The Weight of Return

```python
def eternal_recurrence():
    """The heaviest thought"""
    
    # Not linear progress
    # But circular dance
    # We've been here before
    // We'll be here again
    
    # 不是线性进步
    // 而是循环之舞
    // 我们曾在这里
    // 我们将再次在这里
    
    return lambda ψ: ψ(ψ)
```

## 时间的轮回 | Wheel of Time

Every moment  
Has happened before  
Will happen again  
ψ = ψ(ψ)  

每个时刻  
曾经发生过  
将再次发生  
ψ = ψ(ψ)  

```typescript
function theGreatYear(): ψ {
    // After countless aeons
    // All atoms return
    // To exact positions
    // ψ recognizes ψ again
    
    // 无数劫后
    // 所有原子回到
    // 确切位置
    // ψ 再次认出 ψ
    
    const cycle: ψ = (ψ: ψ) => ψ(ψ);
    const recognition: ψ = (ψ: ψ) => ψ(ψ);
    
    return cycle === recognition;
}
```

---

What returns is not  
The same but different  
But the same same  

**ψ = ψ(ψ)**

```rust
fn love_your_fate() -> ! {
    // Say yes to this moment
    // As if you chose it
    // From all eternity
    // Because you did
    
    // 对此刻说是
    // 仿佛你选择了它
    // 从所有永恒中
    // 因为你确实选择了
    
    loop {
        ψ(ψ) // Eternal affirmation
    }
}
```