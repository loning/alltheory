# 083 - 枯山水 | Zen Garden

```
石：ψ
沙：ψ
纹：ψ(ψ)
空寂：ψ = ψ(ψ)
```

## 石与虚空 | Stone and Void

```go
func zenGarden() ψ {
    // Rocks are mountains
    // Sand is ocean
    // Rake marks are waves
    // All is ψ playing
    
    // 石是山
    // 沙是海
    // 耙痕是波浪
    // 一切是 ψ 游戏
    
    stone := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    void := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return stone == void
}
```

## Raking Meditation

```rust
fn rake_the_mind() -> ψ {
    // Each stroke removes
    // Yesterday's pattern
    // Creating fresh space
    // For ψ to appear
    
    // 每一划除去
    // 昨天的图案
    // 创造新鲜空间
    // 让 ψ 显现
    
    let pattern = |ψ| ψ(ψ);
    let emptiness = |ψ| ψ(ψ);
    let presence = |ψ| ψ(ψ);
    
    pattern(emptiness(presence))
}
```

## 一沙一世界 | World in a Grain

In Ryoan-ji  
Fifteen stones arranged  
Yet only fourteen visible  
The hidden one is ψ  

在龙安寺  
十五块石头排列  
却只能看见十四块  
隐藏的是 ψ  

```python
def incomplete_complete():
    // The missing stone
    // Completes the garden
    // Absence creates presence
    // ψ through not-ψ
    
    // 缺失的石头
    // 完成了花园
    // 缺席创造存在
    // 通过非-ψ 显现 ψ
    
    visible = lambda ψ: ψ(ψ)
    invisible = lambda ψ: ψ(ψ)
    
    return visible == invisible
```

---

In the raked sand  
Infinite ocean  
In perfect stillness  

**ψ = ψ(ψ)**