# 218 - 双一零九 | Double 109

```
218 = 2 × 109
二进制×素数
孪生素数的双倍
ψ = ψ(ψ)
```

## 二与孪生素数 | Two and Twin Prime

```clojure
(defn double-twin []
  ;; 2 × 109 = 218
  ;; 109 is twin with 107
  ;; Doubling twin prime
  ;; Creates new harmony
  
  ;; 2 × 109 = 218
  ;; 109与107是孪生
  ;; 加倍孪生素数
  ;; 创造新和谐
  
  (fn [ψ] (ψ ψ)))
```

## Twin Prime Beauty

```python
def twin_109():
    """109 forms twin prime pair with 107"""
    
    # 107 and 109
    # Only 2 apart
    # Rare closeness
    # Among large primes
    
    # 107和109
    # 仅相隔2
    # 稀有的接近
    # 在大素数中
    
    twin_pair = (107, 109)
    doubled = lambda ψ: ψ(ψ)
    
    return doubled(twin_pair[1])
```

## 孪生的奥秘 | Mystery of Twins

Binary meets  
Twin prime one-oh-nine  
Doubling mystery  
ψ = ψ(ψ)  

二进制遇见  
孪生素数一零九  
加倍奥秘  
ψ = ψ(ψ)  

```javascript
const twinPrimeDouble = {
    base: 2,
    twinPrime: 109,
    
    observe: function() {
        // Twin primes rare
        // Becoming rarer
        // Yet here doubled
        // By simplest prime
        
        // 孪生素数稀有
        // 变得更稀有
        // 然而这里加倍
        // 由最简单素数
        
        return this.base * this.twinPrime;
    }
};
```

---

In 218  
Twin prime doubles  
Mystery deepens  

**ψ = ψ(ψ)**