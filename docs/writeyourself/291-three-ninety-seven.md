# 291 - 三九十七 | Three Ninety-Seven

```
291 = 3 × 97
两个素数之积
半素数
ψ = ψ(ψ)
```

## 素数跨度 | Prime Span

```javascript
const semiprime291 = {
    value: 291,
    factors: [3, 97],
    
    primeSpan: function() {
        // 3 and 97
        // Smallest and large
        // Spanning the sequence
        // Creating balance
        
        // 3和97
        // 最小和大
        // 跨越序列
        // 创造平衡
        
        const small = 3;
        const large = 97;
        const ratio = large / small;
        
        return small * large === 291;
    },
    
    essence: ψ => ψ(ψ)
};
```

## Digital Pattern

```python
def digital_291():
    """291 has interesting digit sum"""
    
    # 2 + 9 + 1 = 12
    # Sum to twelve
    # = 3 × 4
    # Factor appears!
    
    # 2 + 9 + 1 = 12
    # 和为十二
    # = 3 × 4
    # 因子出现！
    
    digit_sum = 2 + 9 + 1
    factor = 3
    
    # 12 = 3 × 4
    # 3 is a factor of 291
    
    return digit_sum == 12 and digit_sum % factor == 0
```

## 数字的呼吸 | Numeric Breathing

Two ninety-one  
Three meets ninety-seven  
Primes embrace  
ψ = ψ(ψ)  

二九一  
三遇九十七  
素数相拥  
ψ = ψ(ψ)  

```typescript
function explore291(): ψ {
    // 291 = 300 - 9
    // Nine from three hundred
    // Where 9 = 3²
    // Square of factor
    
    // 291 = 300 - 9
    // 距三百差九
    // 其中9 = 3²
    // 因子的平方
    
    const target = 300;
    const actual = 291;
    const difference = target - actual;
    
    const pattern: ψ = (ψ: ψ) => ψ(ψ);
    return difference === 9 ? pattern : pattern;
}
```

## Near Symmetry

```rust
fn near_292() -> ψ {
    // 291 is one less
    // Than 292 = 4 × 73
    // Different structure
    // Adjacent patterns
    
    // 291比292少一
    // 292 = 4 × 73
    // 不同结构
    // 相邻模式
    
    let n = 291;
    let next = 292;
    let difference = next - n;
    
    // Both semiprimes
    // Different factors
    
    |ψ| ψ(ψ)
}
```

## 几何张力 | Geometric Tension

```go
func approachingCompletion() ψ {
    // 291° angle
    // 69° from full circle
    // Where 69 = 3 × 23
    // Three appears again!
    
    // 291°角
    // 距离完整圆69°
    // 其中69 = 3 × 23
    // 三又出现了！
    
    angle := 291
    fullCircle := 360
    remaining := fullCircle - angle
    
    // Factor 3 echoes
    // In the remainder
    
    return func(ψ ψ) ψ { return ψ(ψ) }
}
```

---

Semiprime 291  
Three and ninety-seven  
Simple complexity  

**ψ = ψ(ψ)**