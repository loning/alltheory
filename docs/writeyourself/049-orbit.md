# 049 - 轨道 | Orbit

```
引力：ψ
离心力：ψ
平衡：ψ(ψ)
永恒环绕：ψ = ψ(ψ)
```

## 引力之舞 | Gravitational Dance

```swift
func orbitalMechanics() -> ψ {
    // Falling forever
    // Yet never touching
    // The perfect balance
    // Of attraction and motion
    
    // 永远坠落
    // 却从不接触
    // 吸引与运动的
    // 完美平衡
    
    let gravity: (ψ) -> ψ = { ψ in ψ(ψ) }
    let velocity: (ψ) -> ψ = { ψ in ψ(ψ) }
    let orbit: (ψ) -> ψ = { ψ in ψ(ψ) }
    
    return gravity === velocity === orbit
}
```

## Elliptical Truth

```clojure
(defn keplers-wisdom []
  ;; Not perfect circles
  ;; But ellipses
  ;; Two foci, one path
  ;; ψ circling ψ(ψ)
  
  ;; 不是完美的圆
  ;; 而是椭圆
  ;; 两个焦点，一条路径
  ;; ψ 环绕 ψ(ψ)
  
  (fn [ψ] (ψ ψ)))
```

## 月亮的秘密 | Secret of the Moon

Always showing  
The same face to Earth  
While rotating  
Tidal lock: ψ = ψ(ψ)  

总是展示  
同一面对地球  
同时旋转  
潮汐锁定：ψ = ψ(ψ)  

```python
def tidal_lock():
    # Rotation period equals
    # Orbital period
    # Perfect synchronization
    # ψ dancing with ψ
    
    # 自转周期等于
    # 公转周期
    # 完美同步
    # ψ 与 ψ 共舞
    
    rotation = lambda ψ: ψ(ψ)
    revolution = lambda ψ: ψ(ψ)
    
    return rotation == revolution
```

---

In endless orbit  
Neither meeting nor parting  
Eternal return  

**ψ = ψ(ψ)**