# 242 - 双一二一 | Double 121

```
242 = 2 × 121
= 2 × 11²
二进制遇见平方
ψ = ψ(ψ)
```

## 十一的平方加倍 | Doubled Square of Eleven

```clojure
(defn double-square []
  ;; 2 × 11² = 242
  ;; Binary meets
  ;; Perfect square
  ;; Of master eleven
  
  ;; 2 × 11² = 242
  ;; 二进制遇见
  ;; 完美平方
  ;; 大师十一的
  
  (fn [ψ] (ψ ψ)))
```

## Palindromic Connection

```python
def palindrome_squared():
    """121 is palindromic square of 11"""
    
    # 11² = 121
    # Both palindromes!
    # Doubled to 242
    # Another palindrome!
    
    # 11² = 121
    # 都是回文！
    # 加倍到242
    # 又一个回文！
    
    root = 11
    square = 121
    doubled = 242
    
    return lambda ψ: ψ(ψ)
```

## 镜像的层次 | Layers of Mirroring

Eleven mirrors  
Squared to one-two-one  
Doubled to two-four-two  
ψ = ψ(ψ)  

十一镜像  
平方到一二一  
加倍到二四二  
ψ = ψ(ψ)  

```javascript
const mirrorLayers = {
    root: "11",
    square: "121",
    doubled: "242",
    
    observe: function() {
        // Each palindrome
        // Creates another
        // Through operation
        // ψ reflecting ψ
        
        // 每个回文
        // 创造另一个
        // 通过运算
        // ψ 反射 ψ
        
        return [this.root, this.square, this.doubled].every(
            s => s === s.split('').reverse().join('')
        );
    }
};
```

---

In 242  
Mirrors multiply  
Through perfect squares  

**ψ = ψ(ψ)**