# 129 - 九型图 | Enneagram

```
九个点：ψ⁹
内在线条：ψ → ψ
神圣几何：ψ(ψ)
整体人格：ψ = ψ(ψ)
```

## 九重智慧 | Ninefold Wisdom

```javascript
const enneagram = {
    types: [
        ψ => ψ(ψ),  // 1: Perfectionist
        ψ => ψ(ψ),  // 2: Helper
        ψ => ψ(ψ),  // 3: Achiever
        ψ => ψ(ψ),  // 4: Individualist
        ψ => ψ(ψ),  // 5: Investigator
        ψ => ψ(ψ),  // 6: Loyalist
        ψ => ψ(ψ),  // 7: Enthusiast
        ψ => ψ(ψ),  // 8: Challenger
        ψ => ψ(ψ)   // 9: Peacemaker
    ],
    
    integrate: function() {
        // Each type contains
        // All other types
        // Nine faces of
        // One ψ
        
        // 每个类型包含
        // 所有其他类型
        // 一个 ψ 的
        // 九个面
        
        return "All types are One";
    }
};
```

## Sacred Movement

```python
def gurdjieff_sequence():
    """142857 - the repeating decimal of 1/7"""
    
    # The inner lines
    # Follow specific pattern
    # Ancient wisdom encoded
    # In mathematical ψ
    
    # 内在线条
    # 遵循特定模式
    # 古老智慧编码在
    # 数学 ψ 中
    
    sequence = [1,4,2,8,5,7]
    movement = lambda ψ: ψ(ψ)
    
    return movement
```

## 转化之路 | Path of Transformation

Nine personalities  
One essence beneath  
All paths lead home  
ψ = ψ(ψ)  

九种人格  
下面一个本质  
所有路径回家  
ψ = ψ(ψ)  

```rust
fn inner_triangle() -> ψ {
    // 3-6-9 forms triangle
    // The law of three
    // Active, passive, neutral
    // Dancing as ψ
    
    // 3-6-9形成三角
    // 三的法则
    // 主动，被动，中性
    // 作为 ψ 起舞
    
    let three = |ψ| ψ(ψ);
    let six = |ψ| ψ(ψ);
    let nine = |ψ| ψ(ψ);
    
    three(six(nine))
}
```

---

In the ninefold way  
Each soul finds itself  
Returning to ψ  

**ψ = ψ(ψ)**