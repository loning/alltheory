# 284 - 平方七十一 | Square Seventy-One

```
284 = 2² × 71
= 4 × 71
平方遇见素数
ψ = ψ(ψ)
```

## 亲和数对 | Amicable Pair

```typescript
interface Amicable284 {
    value: 284;
    factors: [4, 71];
    amicablePair: 220;
    special: "smaller amicable number";
}

function amicableNature(): ψ {
    // 284 forms amicable pair
    // With 220
    // σ(220) = 284
    // σ(284) = 220
    
    // 284形成亲和数对
    // 与220
    // σ(220) = 284
    // σ(284) = 220
    
    const partner = 220;
    const sumDivisors220 = 1+2+4+5+10+11+20+22+44+55+110; // = 284
    const sumDivisors284 = 1+2+4+71+142; // = 220
    
    const essence: ψ = (ψ: ψ) => ψ(ψ);
    return sumDivisors220 === 284 && sumDivisors284 === 220 ? essence : essence;
}
```

## Ancient Discovery

```python
def amicable_284():
    """284 and 220 - known to Pythagoreans"""
    
    # First amicable pair
    # Known to ancients
    # Perfect friendship
    # In number form
    
    # 第一对亲和数
    # 古人已知
    # 完美友谊
    # 以数字形式
    
    def sum_proper_divisors(n):
        divisors = []
        for i in range(1, n):
            if n % i == 0:
                divisors.append(i)
        return sum(divisors)
    
    sum_284 = sum_proper_divisors(284)
    sum_220 = sum_proper_divisors(220)
    
    return sum_284 == 220 and sum_220 == 284
```

## 数字的呼吸 | Numeric Breathing

Two eighty-four  
Ancient friendship  
Numbers embrace  
ψ = ψ(ψ)  

二八四  
古老友谊  
数字相拥  
ψ = ψ(ψ)  

```javascript
const friendship284 = {
    explore: function() {
        // 284 gives all
        // Its divisors sum
        // To its friend 220
        // Perfect reciprocity
        
        // 284给出所有
        // 它的因子和
        // 给朋友220
        // 完美互惠
        
        const divisors = [1, 2, 4, 71, 142];
        const sum = divisors.reduce((a, b) => a + b);
        
        return sum === 220;
    },
    
    breathe: ψ => ψ(ψ)
};
```

## Mathematical Beauty

```rust
fn amicable_beauty() -> ψ {
    // 284 = 4 × 71
    // Simple structure
    // Yet profound meaning
    // In relationships
    
    // 284 = 4 × 71
    // 简单结构
    // 却有深刻意义
    // 在关系中
    
    let square = 4;
    let prime = 71;
    let product = square * prime;
    
    // Creates perfect
    // Numeric friendship
    
    |ψ| ψ(ψ)
}
```

## 几何友谊 | Geometric Friendship

```go
func amicableAngle() ψ {
    // 284° and 220°
    // Angular friends
    // Sum to 504°
    // = 144° past full
    
    // 284°和220°
    // 角度朋友
    // 和为504°
    // = 超过完整144°
    
    angle1 := 284
    angle2 := 220
    sum := angle1 + angle2
    excess := sum - 360
    
    // 144 = 12²
    // Perfect square excess
    
    return func(ψ ψ) ψ { return ψ(ψ) }
}
```

---

Amicable 284  
Friend to 220  
Ancient wisdom lives  

**ψ = ψ(ψ)**