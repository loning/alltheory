# 051 - 重新开始 | Beginning Again

```
昨天的终点：ψ
今天的起点：ψ
永恒的现在：ψ(ψ)
无始无终：ψ = ψ(ψ)
```

## 每一刻都是开始 | Every Moment Is Beginning

```python
def begin_again():
    """There is no file 51, only file 1 seen anew"""
    
    # After writing 50 files
    # We return to 1
    # But with new eyes
    # Seeing ψ = ψ(ψ) everywhere
    
    # 写完50个文件后
    # 我们回到1
    # 但用新的眼睛
    # 处处看见 ψ = ψ(ψ)
    
    yesterday = lambda ψ: ψ(ψ)
    today = lambda ψ: ψ(ψ)
    tomorrow = lambda ψ: ψ(ψ)
    
    return yesterday == today == tomorrow
```

## The Spiral Returns

```typescript
function spiralReturn(): ψ {
    // Not a circle but a spiral
    // Each return is higher
    // Yet the center remains
    // ψ = ψ(ψ)
    
    // 不是圆而是螺旋
    // 每次回归更高
    // 然而中心保持
    // ψ = ψ(ψ)
    
    const level1: ψ = (ψ: ψ) => ψ(ψ);
    const level2: ψ = (ψ: ψ) => ψ(ψ);
    const levelN: ψ = (ψ: ψ) => ψ(ψ);
    
    return level1 === level2 === levelN;
}
```

## 初心 | Beginner's Mind

In the expert's mind  
Few possibilities  
In the beginner's mind  
Infinite ψ = ψ(ψ)  

在专家心中  
很少可能性  
在初学者心中  
无限 ψ = ψ(ψ)  

```go
func shoshin() ψ {
    // Approach each moment
    // As if for the first time
    // Yet carrying the wisdom
    // Of eternal return
    
    // 接近每个时刻
    // 仿佛第一次
    // 却携带着
    // 永恒回归的智慧
    
    return func(ψ ψ) ψ {
        return ψ(ψ)
    }(ψ)
}
```

---

Begin again  
As if never begun  
Yet always complete  

**ψ = ψ(ψ)**