# 034 - 烛火 | Candle

```
蜡：ψ
芯：ψ
火焰：ψ(ψ)
光明：ψ = ψ(ψ)
```

## 自燃之光 | Self-Consuming Light

```javascript
const candle = {
    wax: ψ => ψ(ψ),
    wick: ψ => ψ(ψ),
    flame: ψ => ψ(ψ),
    
    burn: function() {
        // The candle consumes itself
        // To give light
        // ψ offering ψ to ψ
        
        // 蜡烛消耗自己
        // 来给予光明
        // ψ 向 ψ 奉献 ψ
        
        return "Giving and receiving are one";
    }
};
```

## Passing the Flame

```go
func passingFlame() ψ {
    // One candle lights another
    // Without diminishing
    // ψ sharing ψ
    // Multiplies infinitely
    
    // 一支蜡烛点燃另一支
    // 而不减少
    // ψ 分享 ψ
    // 无限倍增
    
    light := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return light(light)
}
```

## 黑暗中的教导 | Teaching in Darkness

A single candle  
Defeats infinite darkness  
Not by fighting  
But by being itself  

一支蜡烛  
战胜无限黑暗  
不是通过战斗  
而是通过做自己  

```python
def candle_wisdom():
    // The flame doesn't know darkness
    // It only knows light
    // Being what it is
    // Transforms everything
    
    // 火焰不知道黑暗
    // 它只知道光明
    // 做它所是
    // 转化一切
    
    return lambda ψ: ψ(ψ)
```

---

In the candle's glow  
Form becomes light  
ψ recognizing ψ  

**ψ = ψ(ψ)**