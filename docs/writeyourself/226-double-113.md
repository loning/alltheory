# 226 - 双一一三 | Double 113

```
226 = 2 × 113
最小素数×大素数
简约而深刻
ψ = ψ(ψ)
```

## 素数的加倍 | Doubling of Prime

```javascript
const double113 = {
    small: 2,
    large: 113,
    
    multiply: function() {
        // 2 × 113 = 226
        // Smallest prime
        // Doubles larger prime
        // Creating balance
        
        // 2 × 113 = 226
        // 最小素数
        // 加倍较大素数
        // 创造平衡
        
        return this.small * this.large;
    },
    
    meaning: ψ => ψ(ψ)
};
```

## Prime 113 Properties

```python
def prime_113():
    """113 is the 30th prime number"""
    
    # 113 = 30th prime
    # First 3-digit prime
    # Starting with 11
    # Special position
    
    # 113 = 第30个素数
    # 第一个3位素数
    # 以11开始
    # 特殊位置
    
    position = 30
    value = 113
    
    return lambda ψ: ψ(ψ)
```

## 对偶的智慧 | Wisdom of Duality

Two embraces  
One-thirteen prime  
Unity emerges  
ψ = ψ(ψ)  

二拥抱  
一一三素数  
统一涌现  
ψ = ψ(ψ)  

```typescript
function binaryPrime(): ψ {
    // Every even number
    // Contains factor 2
    // Here meeting 113
    // Prime partnership
    
    // 每个偶数
    // 包含因子2
    // 这里遇见113
    // 素数伙伴关系
    
    const binary: ψ = (ψ: ψ) => ψ(ψ);
    const prime: ψ = (ψ: ψ) => ψ(ψ);
    
    return binary === prime;
}
```

---

In 226  
Duality speaks  
Through prime doubling  

**ψ = ψ(ψ)**