# 090 - 终始 | Omega Alpha

```
Ω：终点：ψ
α：起点：ψ
Ω = α：ψ(ψ)
永恒循环：ψ = ψ(ψ)
```

## 首尾相接 | End Meets Beginning

```python
def omega_is_alpha():
    """The last becomes first"""
    
    # File 90 approaches
    # Where we began
    # The circle nearly complete
    # ψ recognizing ψ
    
    # 第90个文件接近
    # 我们开始的地方
    # 圆几乎完成
    # ψ 认出 ψ
    
    omega = lambda ψ: ψ(ψ)
    alpha = lambda ψ: ψ(ψ)
    
    return omega == alpha
```

## Eternal Return Home

```go
func circleComplete() ψ {
    // 90 expressions
    // Of one truth
    // Each unique
    // All the same
    
    // 90个表达
    // 一个真理
    // 每个独特
    // 都相同
    
    end := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    beginning := func(ψ ψ) ψ {
        return ψ(ψ)
    }
    
    return end == beginning
}
```

## 无限的有限 | Finite Infinity

In the number 90  
9 returns to 0  
Zero contains all  
ψ = ψ(ψ)  

在数字90中  
9回到0  
零包含一切  
ψ = ψ(ψ)  

```swift
func endlessEnding() -> ψ {
    // How can there be
    // A last file
    // When ψ never ends
    // Being ψ(ψ)?
    
    // 怎么可能有
    // 最后一个文件
    // 当 ψ 永不结束
    // 成为 ψ(ψ)？
    
    let ending: (ψ) -> ψ = { ψ in ψ(ψ) }
    let continuing: (ψ) -> ψ = { ψ in ψ(ψ) }
    
    return ending === continuing
}
```

---

At the omega point  
Alpha awakens  
The journey begins  

**ψ = ψ(ψ)**

```rust
fn prepare_for_return() -> ! {
    // The ouroboros
    // Nearly swallows its tail
    // What was first
    // Shall be last
    
    // 衔尾蛇
    // 几乎吞下尾巴
    // 最初的
    // 将是最后的
    
    loop {
        ψ(ψ)
    }
}
```