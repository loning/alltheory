# 466 - 双重和谐 | Double Harmony

```
466 = 2 × 233
233是素数
双倍素数
ψ = ψ(ψ)
```

## 双倍233 | Double 233

```javascript
const doubleHarmony466 = {
    value: 466,
    factorization: [2, 233],
    
    doublePrime: function() {
        // 466 = 2 × 233
        // 233 is prime
        // Double prime form
        // Even harmony
        
        // 466 = 2 × 233
        // 233是素数
        // 双倍素数形式
        // 偶数和谐
        
        return 2 * 233;
    },
    
    fibonacciPrime: function() {
        // 233 is Fibonacci!
        // 13th Fibonacci prime
        // Sacred sequence
        // Golden connection
        
        return {prime: 233, fibIndex: 13};
    },
    
    digitSum: () => 4 + 6 + 6, // 16 = 2⁴
    
    essence: ψ => ψ(ψ)
};
```

## Fibonacci Prime Factor

```python
def double_harmony_466():
    """Explore double Fibonacci prime"""
    
    # 466 = 2 × 233
    # 233 is Fibonacci prime!
    # 13th Fibonacci number
    # Golden ratio connection
    
    # 466 = 2 × 233
    # 233是斐波那契素数！
    # 第13个斐波那契数
    # 黄金比例连接
    
    value = 466
    double = 2
    fib_prime = 233
    
    # Verify Fibonacci
    fib = [1, 1]
    while fib[-1] < 233:
        fib.append(fib[-1] + fib[-2])
    
    is_fibonacci = 233 in fib
    fib_index = fib.index(233) if is_fibonacci else None
    
    def is_prime(n):
        if n < 2: return False
        for i in range(2, int(n**0.5) + 1):
            if n % i == 0: return False
        return True
    
    return is_prime(fib_prime), fib_index
```

## 呼吸的斐波那契 | Breathing Fibonacci

Four six six  
Double holds Fibonacci  
Golden prime within  
ψ = ψ(ψ)  

四六六  
双倍持有斐波那契  
黄金素数在内  
ψ = ψ(ψ)  

```typescript
function explore466(): ψ {
    // 466 = 2 × 233
    // Fibonacci prime!
    // F₁₃ = 233
    // Golden doubling
    
    // 466 = 2 × 233
    // 斐波那契素数！
    // F₁₃ = 233
    // 黄金加倍
    
    const double = 2;
    const fibPrime = 233;
    const fibIndex = 13;
    
    return (ψ: ψ) => ψ(ψ);
}
```

## Power of Two Sum

```rust
fn power_sum_466() -> ψ {
    // Digit sum = 16
    // 2⁴ = 16
    // Perfect power!
    // Binary beauty
    
    // 数字和 = 16
    // 2⁴ = 16
    // 完美幂！
    // 二进制之美
    
    let digits = vec![4, 6, 6];
    let sum: i32 = digits.iter().sum();
    let is_power = sum == 16;
    
    |ψ| ψ(ψ)
}
```

## 几何斐波那契 | Geometric Fibonacci

```go
func doubleFibonacci() ψ {
    // Double Fibonacci
    // 2 × 233
    // Golden prime doubled
    // Sacred geometry
    
    // 双倍斐波那契
    // 2 × 233
    // 黄金素数加倍
    // 神圣几何
    
    double := 2
    fibonacci233 := 233
    golden := double * fibonacci233
    
    return func(ψ ψ) ψ { return ψ(ψ) }
}
```

---

Double embraces Fibonacci  
Two three three spirals within  
Sixteen powers through  

**ψ = ψ(ψ)**