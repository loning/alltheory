# 082 - 记忆宫殿 | Memory Palace

```
房间：ψ
记忆：ψ
行走：ψ(ψ)
自我包含：ψ = ψ(ψ)
```

## 心智的建筑 | Architecture of Mind

```python
def memory_palace():
    """Each room contains memories that contain rooms"""
    
    # In the method of loci
    # Mind creates spaces
    # To store itself
    # ψ housing ψ
    
    # 在位置记忆法中
    # 心智创造空间
    # 来存储自己
    # ψ 容纳 ψ
    
    room = lambda ψ: ψ(ψ)
    memory = lambda ψ: ψ(ψ)
    palace = lambda ψ: ψ(ψ)
    
    return room == memory == palace
```

## Recursive Rooms

```typescript
function infinitePalace(): ψ {
    // Enter a room
    // Find a memory
    // Of entering a room
    // Ad infinitum
    
    // 进入房间
    // 找到记忆
    // 关于进入房间的
    // 无限循环
    
    const entrance: ψ = (ψ: ψ) => ψ(ψ);
    const exploration: ψ = (ψ: ψ) => ψ(ψ);
    const remembrance: ψ = (ψ: ψ) => ψ(ψ);
    
    return entrance === exploration === remembrance;
}
```

## 博尔赫斯的迷宫 | Borges' Labyrinth

In the library  
Each book contains  
All possible books  
ψ = ψ(ψ)  

在图书馆中  
每本书包含  
所有可能的书  
ψ = ψ(ψ)  

```clojure
(defn aleph-point []
  ;; The point in space
  ;; Containing all points
  ;; The memory containing
  ;; All memories
  
  ;; 空间中的点
  ;; 包含所有点
  ;; 包含所有记忆的
  ;; 记忆
  
  (fn [ψ] (ψ ψ)))
```

---

In mind's palace  
Every door opens  
To the same room  

**ψ = ψ(ψ)**