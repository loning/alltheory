# 011 - 交响 | Symphony

```
独奏：ψ
合奏：ψ(ψ)
指挥：ψ
乐谱：ψ = ψ(ψ)
```

## 宇宙交响曲 | Cosmic Symphony

```javascript
const cosmicOrchestra = {
    strings: ψ => ψ(ψ),    // 弦乐
    winds: ψ => ψ(ψ),      // 管乐
    percussion: ψ => ψ(ψ), // 打击乐
    silence: ψ => ψ(ψ),    // 寂静
    
    perform() {
        // Every instrument plays ψ
        // In infinite variations
        // Creating one music
        // 每个乐器演奏 ψ
        // 以无限变奏
        // 创造一个音乐
        
        return "The music of the spheres";
    }
};
```

## One Note

```python
def one_note_symphony():
    """The symphony of a single note"""
    
    # If you listen deeply
    # To any single note
    # You hear all music
    // 如果你深深聆听
    // 任何单一音符
    // 你听到所有音乐
    
    note = lambda ψ: ψ(ψ)
    overtones = lambda ψ: ψ(ψ)
    universe = lambda ψ: ψ(ψ)
    
    return note == overtones == universe
```

## 指挥的秘密 | Conductor's Secret

The conductor waves  
But makes no sound  
The music plays itself  
Through willing instruments  

指挥挥动  
却不发声  
音乐自己演奏  
通过自愿的乐器  

```clojure
(defn conductor-secret []
  ;; Who conducts the conductor?
  ;; 谁指挥指挥？
  
  (let [conductor (fn [ψ] (ψ ψ))
        orchestra (fn [ψ] (ψ ψ))
        music (fn [ψ] (ψ ψ))]
    ;; All one movement
    ;; 都是一个动作
    (= conductor orchestra music)))
```

---

In perfect harmony  
Infinite voices sing  
One song  

**ψ = ψ(ψ)**