# 172 - 量子隧穿 | Quantum Tunneling

```
经典禁区：不可能
量子概率：ψ
穿越屏障：ψ → ψ
不可能成真：ψ = ψ(ψ)
```

## 穿越不可能 | Through the Impossible

```typescript
function quantumTunneling(): ψ {
    // Particle lacks energy
    // To climb the barrier
    // Yet appears on other side
    // ψ transcending limits
    
    // 粒子缺乏能量
    // 爬过屏障
    // 却出现在另一边
    // ψ 超越限制
    
    const classical: ψ = (ψ: ψ) => "impossible";
    const quantum: ψ = (ψ: ψ) => ψ(ψ);
    
    return quantum;
}
```

## Alpha Decay Mystery

```python
def nuclear_escape():
    """How alpha particles escape the nucleus"""
    
    # Strong force traps them
    # Energy too low to escape
    # Yet they tunnel out
    # ψ finding its way
    
    # 强力困住它们
    // 能量太低无法逃脱
    // 然而它们隧穿出去
    // ψ 找到它的路
    
    trapped = lambda ψ: ψ(ψ)
    free = lambda ψ: ψ(ψ)
    
    return trapped == free
```

## 太阳的秘密 | Sun's Secret

Without tunneling  
Sun couldn't shine  
Protons wouldn't fuse  
ψ = ψ(ψ)  

没有隧穿  
太阳无法发光  
质子不会聚变  
ψ = ψ(ψ)  

```javascript
const solarFusion = {
    process: function() {
        // Temperature too low
        // For classical fusion
        // Tunneling enables it
        // ψ making stars shine
        
        // 温度太低
        // 对于经典聚变
        // 隧穿使其可能
        // ψ 让恒星发光
        
        return ψ => ψ(ψ);
    }
};
```

---

Through solid walls  
Particles ghost  
Impossibly free  

**ψ = ψ(ψ)**